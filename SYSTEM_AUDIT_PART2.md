# System Audit — 三、修复路线图 & 目标架构

---

## 一、修复路线图

### P0-1: bd-worker 僵尸进程清理

**原因**: 22 个进程已积压，每次 server crash 再 +1。不修的话几周后 PID 耗尽 → 整个 server 启动不了。影响面最大、修复最简单。

**修复**: 
1. 清理现有：`pkill -f bd-worker`
2. 修复 server.js:197：重启前 `oldWorker.kill()`
3. 加固：crash > 3 次/分钟 → 停止重启，报警

---

### P0-2: 基础监控

**原因**: 现在任何组件挂了都不知道——新闻停 22 小时全靠审计发现。监控是 P0 因为它决定"问题发生多久后才知道"。

**修复**: 
1. 加一个 healthcheck cron (`*/5 * * * *`)
2. 检查：server进程/Binance可达/新闻更新/磁盘/僵尸进程
3. 异常 → 写日志 `/tmp/health.log`（现阶段不用外发报警）

---

### P1: API 统一错误处理 + 新闻/异动稳定性

**原因**: CoinGecko 挂了静默失败、新闻 cron 断过 22 小时。不影响核心交易，但数据可靠性差。

**修复**:
1. 所有 API 调用加 `try/catch + fallback` 标记
2. 新闻/异动 cron 加"最后更新时间"检查（超过期望间隔 → 日志告警）
3. 数据页面显示"数据新鲜度"而非静默展示旧数据

---

### P2: 存储规范化

**原因**: 新闻/异动/鲸鱼是覆盖式存储，无法回放。暂不影响运行，但长期研究受限。

**修复**:
1. 新闻改按日期存储
2. 异动加时间戳历史
3. klines_cache 确认不会无限增长（528×400×~2KB = ~400MB 上限，安全）

---

## 二、目标架构（当前 → 目标）

```
当前                              目标

无进程管理                        systemd/pm2 管理所有进程
22个僵尸进程                      单实例保护
cron 散落 crontab                 统一 task-scheduler
无监控                            5分钟 healthcheck
无报警                            日志告警（→ 未来：Telegram/微信）
API 静默失败                      统一 error handler + 降级标记
数据覆盖式存储                     所有重要数据按日期快照
无进程隔离                         原站/量化站完全独立启动
```

### 目标架构图

```
┌──────────────────────────────────────────────────────┐
│                   process-manager                     │
│            (pm2 或 systemd 管理所有 Node)              │
├──────────────────────────────────────────────────────┤
│                                                       │
│  ┌──────────────┐  ┌──────────────┐                   │
│  │  原站 :3001  │  │ 量化站 :3002  │  独立进程组       │
│  │  server.js   │  │ quant-server │                   │
│  └──────┬───────┘  └──────┬───────┘                   │
│         │                  │                           │
│  ┌──────┴──────────────────┴───────┐                   │
│  │        共享数据层               │                   │
│  │  klines_cache/ market_data/     │                   │
│  │  news/ alerts/ whale/           │                   │
│  └─────────────────────────────────┘                   │
│                                                       │
│  ┌──────────────────────────────────┐                  │
│  │       task-scheduler (cron)      │                  │
│  │  采集 · 快照 · 回测 · healthcheck │                  │
│  └──────────────────────────────────┘                  │
│                                                       │
│  ┌──────────────┐  ┌──────────────┐                   │
│  │  healthcheck │  │   alert-log  │  监控层           │
│  │  (每5分钟)   │  │   (日志告警) │                   │
│  └──────────────┘  └──────────────┘                   │
└──────────────────────────────────────────────────────┘
```

---

## 三、原站与量化站隔离审计

### 共享资源

| 资源 | 原站 | 量化站 | 风险 |
|------|------|--------|------|
| klines_cache | ✅ 读取 | ✅ 读取 | ✅ 只读共享, 安全 |
| market_data | ❌ 不用 | ✅ 读取+写入 | ⚠️ 量化站写入, 原站不受影响 |
| node_modules | ✅ | ✅ | ⚠️ 共享依赖, 版本冲突风险 |
| 同一服务器 | ✅ | ✅ | ⚠️ 内存/CPU竞争 |
| .git | ✅ | ✅ | ⚠️ 协作冲突 |

### 独立资源

| 资源 | 原站 | 量化站 |
|------|------|--------|
| 端口 | 3001 (nginx→8080) | 3002 |
| 进程 | server.js | quant-server.js |
| 前端 | public/ | public-quant/ |
| 隧道 | trycloudflare #1 | trycloudflare #2 |
| cron | 自身的 crontab 条目 | 数据层 cron 条目 |

### 建议隔离

| 优先级 | 措施 |
|--------|------|
| P1 | quant-server 引用原站数据用**绝对路径**（已是）, 确保不依赖原站进程 |
| P1 | 分别用不同 pm2/systemd unit, 互不干涉 |
| P2 | 量化站独立 package.json（如果要装新依赖） |
| 不需要 | 分离数据目录——共享数据层是设计意图 |
